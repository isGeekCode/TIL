# RN_시작_002_내_컴퓨터에서_실행하기

## 🤔 이 문서를 읽기 전에
- **선수 지식**: `RN_시작_001_프로젝트_첫_만남.md` 읽음
- **예상 소요 시간**: 1-2시간 (설치 시간 포함)
- **준비물**: 맥북 (iOS 개발 시), 또는 윈도우/리눅스 (Android만)

## 🎯 이 문서에서 배울 것
1. 개발 환경을 왜 설정해야 하는지
2. Node.js, npm이 무엇이고 왜 필요한지
3. Metro Bundler가 뭔지
4. 실제로 앱을 내 폰/에뮬레이터에서 실행하기

---

## 📖 본문

### 1. 왜 이렇게 많이 설치해야 하나요?

#### React Native 앱이 실행되기까지의 과정

```
1. 개발자가 TypeScript/JavaScript 코드 작성
   ↓
2. Metro Bundler가 코드를 번들링 (하나로 묶음)
   ↓
3. React Native가 JavaScript → Native 변환
   ↓
4. iOS/Android 네이티브 앱으로 실행
```

**각 단계마다 다른 도구가 필요합니다!**

#### 필요한 도구들 (비유로 이해하기)

| 도구 | 역할 | 비유 |
|------|------|------|
| **Node.js** | JavaScript 실행 환경 | 주방 (요리하는 공간) |
| **npm** | 패키지 관리자 | 마트 (재료 사는 곳) |
| **Metro Bundler** | 코드 번들러 | 믹서기 (재료 섞는 도구) |
| **Android Studio** | Android 개발 도구 | 안드로이드 공장 |
| **Xcode** | iOS 개발 도구 | 애플 공장 |
| **CocoaPods** | iOS 의존성 관리 | iOS 부품 창고 |

---

### 2. Node.js와 npm 설치하기

#### Node.js가 뭔가요?

**한 줄 요약:**
> JavaScript를 브라우저 밖에서도 실행할 수 있게 해주는 프로그램

**왜 필요한가:**
- React Native는 JavaScript/TypeScript로 작성됨
- 개발 도구(Metro 등)도 JavaScript로 만들어짐
- → Node.js가 없으면 아무것도 실행 안 됨

#### npm이 뭔가요?

**한 줄 요약:**
> Node Package Manager - JavaScript 라이브러리를 설치/관리하는 도구

**예시:**
```bash
# react-native-webview 라이브러리 설치
npm install react-native-webview

# = 마트에서 "react-native-webview"라는 재료를 사서 프로젝트에 추가
```

#### 설치 방법

**macOS:**
```bash
# Homebrew로 설치 (권장)
brew install node

# 버전 확인
node --version   # v22.x.x 이상이어야 함
npm --version    # 10.x.x 이상
```

**Windows:**
1. [Node.js 공식 사이트](https://nodejs.org/) 접속
2. LTS 버전 다운로드
3. 설치 마법사 따라하기
4. 터미널에서 버전 확인

**Linux:**
```bash
# Ubuntu/Debian
curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -
sudo apt-get install -y nodejs

# 버전 확인
node --version
npm --version
```

---

### 3. 프로젝트 클론 및 의존성 설치

#### Git Clone

```bash
# 터미널 열기

# 원하는 폴더로 이동
cd ~/Documents

# 프로젝트 클론 (실제 저장소 URL로 교체)
git clone <repository-url>

# 프로젝트 폴더로 이동
cd myapp_source
```

#### npm install - 마법의 명령어

```bash
# package.json에 적힌 모든 라이브러리 설치
npm install

# 또는
npm i
```

**무슨 일이 일어나나?**
1. `package.json` 파일 읽음
2. `dependencies`에 적힌 패키지들 다운로드
3. `node_modules/` 폴더에 저장

**예상 시간:** 3-10분 (인터넷 속도에 따라)

**설치되는 패키지 예시:**
```json
{
  "dependencies": {
    "react": "19.1.0",
    "react-native": "0.80.2",
    "react-native-webview": "^13.15.0",
    "@react-native-firebase/app": "^23.1.1",
    // ... 총 20여 개
  }
}
```

---

### 4. iOS 추가 설정 (macOS만)

#### CocoaPods 설치

**CocoaPods가 뭔가요?**
> iOS 라이브러리 의존성 관리 도구 (npm의 iOS 버전)

```bash
# Ruby 기반이므로 gem으로 설치
sudo gem install cocoapods

# 버전 확인
pod --version
```

#### Pod Install

```bash
# iOS 폴더로 이동
cd ios

# 의존성 설치
pod install

# 다시 루트로
cd ..
```

**무슨 일이 일어나나?**
1. `Podfile` 읽음
2. 필요한 iOS 라이브러리 다운로드
3. `ios/Pods/` 폴더에 저장
4. `MyApp.xcworkspace` 파일 생성

**⚠️ 주의:**
- `MyApp.xcodeproj` (X) - 이걸 열면 안 됨!
- `MyApp.xcworkspace` (O) - 이걸 열어야 함!

---

### 5. Metro Bundler 이해하기

#### Metro가 뭔가요?

**한 줄 요약:**
> React Native 전용 JavaScript 번들러 (Webpack의 RN 버전)

**역할:**
1. 여러 개의 `.js`, `.ts`, `.tsx` 파일을 하나로 합침
2. 코드 변경 감지 → 자동 재번들링 (핫 리로드)
3. 에러 발생 시 빨간 화면 표시

#### Metro 실행하기

```bash
# 터미널에서 실행
npm start

# 또는
npx react-native start
```

**실행되면 이렇게 보임:**
```
               ######                ######
             ###     ####        ####     ###
            ##          ###    ###          ##
            ##             ####             ##
            ##             ####             ##
            ##           ##    ##           ##
            ##         ###      ###         ##
             ##  ########################  ##
          ######    ###            ###    ######
      ###     ##    ##              ##    ##     ###
   ###         ## ###      ####      ### ##         ###
  ##           ####      ########      ####           ##
 ##             ###     ##########     ###             ##
  ##           ####      ########      ####           ##
   ###         ## ###      ####      ### ##         ###
      ###     ##    ##              ##    ##     ###
          ######    ###            ###    ######
             ##  ########################  ##
            ##         ###      ###         ##
            ##           ##    ##           ##
            ##             ####             ##
            ##             ####             ##
            ##          ###    ###          ##
             ###     ####        ####     ###
               ######                ######

                 Welcome to Metro!

              Fast - Scalable - Integrated


 BUNDLE  ./index.js ░░░░░░░░░░░░░░░░ 100.0% (1/1), done.

  💡 Metro is running
```

**이 터미널은 계속 켜두세요!**
- 앱이 실행되는 동안 Metro가 계속 돌아가야 함
- 코드 수정할 때마다 Metro가 자동으로 재번들링
- 종료하려면 `Ctrl + C`

---

### 6. Android 앱 실행하기

#### 안드로이드 스튜디오 설치

1. [Android Studio 다운로드](https://developer.android.com/studio)
2. 설치 후 초기 설정
3. Android SDK 설치 (자동으로 진행됨)

#### 에뮬레이터 생성

```
Android Studio 열기
→ More Actions
→ Virtual Device Manager
→ Create Device
→ Pixel 5 선택
→ System Image 선택 (Android 13 권장)
→ Finish
```

#### 에뮬레이터 실행

**GUI로 실행:**
```
Virtual Device Manager
→ 생성한 기기 옆 ▶️ 버튼 클릭
```

**또는 명령어로:**
```bash
# 사용 가능한 에뮬레이터 목록 확인
emulator -list-avds

# 에뮬레이터 실행
emulator -avd Pixel_5_API_33
```

#### 앱 빌드 및 실행

**새 터미널 열기** (Metro는 계속 켜두고!)

```bash
# 개발 환경으로 실행
npm run android:dev

# 또는 직접 명령어
npx react-native run-android --mode=devDebug
```

**무슨 일이 일어나나?**
```
1. Gradle 빌드 시작 (Android 앱 컴파일)
2. APK 파일 생성
3. 에뮬레이터에 APK 설치
4. 앱 자동 실행
```

**예상 시간:**
- 첫 빌드: 5-10분
- 이후 빌드: 1-3분

**성공하면:**
- 에뮬레이터에 앱 아이콘 생김
- 앱이 자동으로 열림
- 로딩 후 WebView 화면 보임

---

### 7. iOS 앱 실행하기 (macOS만)

#### Xcode 설치

```bash
# App Store에서 Xcode 검색 후 설치
# 또는 터미널에서
xcode-select --install
```

**용량 주의:** Xcode는 약 15GB입니다!

#### 시뮬레이터 실행

```bash
# 시뮬레이터 앱 실행
open -a Simulator

# 또는 Xcode에서
Xcode → Window → Devices and Simulators
→ Simulators 탭
→ + 버튼으로 새 시뮬레이터 생성
```

**권장 기기:** iPhone 14 (iOS 17.0)

#### 앱 빌드 및 실행

**새 터미널 열기**

```bash
# 개발 환경으로 실행
npm run ios:dev

# 또는 직접 명령어
npx react-native run-ios
```

**무슨 일이 일어나나?**
```
1. CocoaPods 확인
2. Xcode 프로젝트 빌드
3. .app 파일 생성
4. 시뮬레이터에 설치
5. 앱 자동 실행
```

**예상 시간:**
- 첫 빌드: 3-5분
- 이후 빌드: 30초 - 1분

---

### 8. 실제 기기에서 실행하기

#### Android 실제 기기

1. **USB 디버깅 활성화**
   ```
   설정 → 휴대전화 정보 → 빌드 번호 7번 탭
   → 개발자 옵션 활성화
   → USB 디버깅 ON
   ```

2. **USB로 연결**

3. **기기 확인**
   ```bash
   adb devices
   # List of devices attached
   # 1234567890ABCDEF    device
   ```

4. **앱 실행**
   ```bash
   npm run android:dev
   ```

#### iOS 실제 기기

1. **Apple Developer 계정 필요** (무료 계정도 가능, 7일마다 재설치 필요)

2. **Xcode에서 팀 설정**
   ```
   Xcode 열기
   → Signing & Capabilities
   → Team 선택
   ```

3. **기기 신뢰 설정**
   ```
   설정 → 일반 → VPN 및 기기 관리
   → 개발자 앱 → 신뢰
   ```

4. **빌드 및 실행**
   ```bash
   npm run ios:dev
   ```

---

## 💡 직접 해보기

### 실습 1: Metro Bundler 실행하기

```bash
# 1. 터미널 열기
# 2. 프로젝트 폴더로 이동
cd myapp_source

# 3. Metro 실행
npm start

# 4. 성공하면 "Welcome to Metro!" 보임
```

### 실습 2: 코드 변경 후 핫 리로드 확인

1. **Metro 실행 상태 유지**

2. **새 터미널 열어서 앱 실행**
   ```bash
   npm run android:dev
   # 또는
   npm run ios:dev
   ```

3. **앱이 뜬 상태에서 코드 수정**
   ```typescript
   // src/screens/WebShell.tsx 파일 열기
   // LoadingView 컴포넌트 찾기

   <LoadingView />

   // 위에 텍스트 추가
   <Text>테스트 중입니다!</Text>
   <LoadingView />
   ```

4. **파일 저장 (Cmd+S / Ctrl+S)**

5. **앱이 자동으로 새로고침됨** - 이게 핫 리로드!

### 실습 3: console.log 확인하기

1. **WebShell.tsx 파일에 로그 추가**
   ```typescript
   useEffect(() => {
     console.log('🎉 WebShell 컴포넌트가 마운트되었습니다!');
   }, []);
   ```

2. **Metro 터미널에서 로그 확인**
   ```
   LOG  🎉 WebShell 컴포넌트가 마운트되었습니다!
   ```

3. **Chrome DevTools로 확인** (더 자세한 로그)
   - 앱에서 개발자 메뉴 열기 (Android: Cmd+M, iOS: Cmd+D)
   - "Debug" 선택
   - 크롬 브라우저 자동으로 열림
   - F12로 콘솔 확인

---

## 🐛 자주 하는 실수 및 해결

### ❌ 에러 1: "Metro Bundler can't listen on port 8081"

**원인:** 이미 Metro가 실행 중이거나 다른 프로그램이 8081 포트 사용 중

**해결:**
```bash
# 방법 1: 실행 중인 Metro 찾아서 종료
lsof -ti:8081 | xargs kill -9

# 방법 2: 다른 포트 사용
npx react-native start --port 8082
```

### ❌ 에러 2: "Unable to resolve module"

**원인:** node_modules가 제대로 설치 안 됨

**해결:**
```bash
# 1. node_modules 삭제
rm -rf node_modules

# 2. 캐시 삭제
npm cache clean --force

# 3. 재설치
npm install
```

### ❌ 에러 3: "Build failed" (Android)

**원인:** Gradle 캐시 문제

**해결:**
```bash
cd android
./gradlew clean
cd ..
npm run android:dev
```

### ❌ 에러 4: "Pod install failed" (iOS)

**원인:** CocoaPods 캐시 문제

**해결:**
```bash
cd ios

# 기존 Pods 삭제
rm -rf Pods Podfile.lock

# 재설치
pod install

cd ..
npm run ios:dev
```

### ❌ 에러 5: "command not found: npx"

**원인:** Node.js가 제대로 설치 안 됨

**해결:**
```bash
# Node.js 재설치
brew reinstall node  # macOS
# 또는 공식 사이트에서 다운로드
```

---

## 🔗 참고 자료

### 공식 문서
- [React Native 환경 설정](https://reactnative.dev/docs/set-up-your-environment)
- [Metro Bundler](https://metrobundler.dev/)
- [Android Studio 설치](https://developer.android.com/studio)
- [Xcode 설치](https://developer.apple.com/xcode/)

### 유용한 링크
- [React Native 공식 GitHub](https://github.com/facebook/react-native)
- [Stack Overflow - React Native 태그](https://stackoverflow.com/questions/tagged/react-native)

### 관련 문서
- 이전: `RN_시작_001_프로젝트_첫_만남.md`
- 다음: `RN_시작_003_코드_수정해보기.md`
- 핵심: `RN_WebView_001_WebView_기본_개념.md`

---

## ✅ 체크리스트

이 문서를 다 읽었다면 체크해보세요:

- [ ] Node.js와 npm을 설치했다
- [ ] npm install로 의존성을 설치했다
- [ ] Metro Bundler를 실행할 수 있다
- [ ] Android 또는 iOS 앱을 에뮬레이터/시뮬레이터에서 실행했다
- [ ] 코드 수정 후 핫 리로드를 경험했다
- [ ] console.log를 확인할 수 있다
- [ ] 자주 나는 에러들의 해결 방법을 알겠다

---

## 📌 핵심 요약

1. **Node.js + npm = 필수** (JavaScript 실행 환경 + 패키지 관리)
2. **Metro Bundler = 코드 번들러** (계속 켜두어야 함)
3. **npm install = 의존성 설치** (최초 1회 + 새 패키지 추가 시)
4. **iOS는 pod install도 필요** (CocoaPods 의존성)
5. **핫 리로드 = 자동 새로고침** (개발 효율 UP)

---

**다음 문서:** `RN_WebView_001_WebView_기본_개념.md`로 드디어 WebView를 배웁니다!

**작성일**: 2026-01-07
**난이도**: 🟢 입문
